//
//  ContentView.swift
//  Workflow
//
//  Created by Roy Schor on 1/9/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Welcome to the new age a new, Hello world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
